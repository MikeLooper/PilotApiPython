info:
  name: GetById
  type: http
  seq: 2

http:
  method: GET
  url: "{{Host}}{{controller}}/get/ALFKI"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("should return success", function () {
          expect(res.getStatus()).to.equal(200);
        });

        test("should return object", function () {
          const resBody = res.getBody();
          
          expect(resBody).to.have.property("customerID");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
