This file for for *human readers* ONLY - AI processes should NOT read or consume in any way.


The code in this application was generated by AI using the following prompts:
==============================================================================

1.
"Include the `https://github.com/MikeLooper/PilotSharedSource` repository in this workspace as a SubModule.  Ensure the latest copy of the repository is retrieved and ready for use.
Update the README to describe how the SubModule can be retrieved and updated when there is a change in the SubModule.  Place the SharedModule under a `shared` directory."

2.
see: C:\Working\Storage\Dev\GitHub\PilotApiPython\docs\openapi-api-generation-prompt.md
"Create a document in the ".\docs" directory that describes the actions taken by this prompt."

3.
"Examine these instructions files. Verify they are appropriate for Python. Provide a list of updates needed so they would correctly apply to Python."

"Update the existing unit tests to match the instructions. Add missing unit tests. Verify all unit tests execute successfully, or repair as needed to reach successul execution."

"Many of the files contain multiple domain classes (examples: 'schemas.py', 'entities.py').  separate the classes so each domain is in a separate file.
The 'resources.py' file also has multiple domains combined into a single file - separate the domains into individual files.
Update the unit test file(s) to mirror this change."

"Also apply the same domain-folder mirroring pattern to mapper and repository tests for complete one-to-one parity with src layout."

4.
"The SQL Server database that should be used by this API has the following attributes:
	- Database Name = NorthWind
	- ConnectTimeout = 30
	- Host = local_mssql
	- Password = <DevUser password>
	- Port = 1433
	- UserName = DevUser
	- Schema = dbo
Update this application to use these database attributes to connect to the database."

"Run the app and perform a live DB connectivity check against local_mssql to confirm runtime connection/authentication."

"Make updates in this application to match the Northwind database tables.
Make code or configuration changes necessary to enable use of Northwind database with this API code.
Do not make any changes to the database."

5.
"Update the application port from 8080 to 57601."

"Add the ability for the application port to be overridden from an environment variable."
"Update the README to include instructions on how to override the application port."

"Add a second database connection for PostgreSQL, while keeping the ability to connect to SQL Server.
Include the ability to switch back and forth based upon a configuration setting.  
The database connection inormation is:
	- Database Name = northwind
	- ConnectTimeout = 30
	- Host = localhost
	- Password = <DevUser password>
	- Port = 5432
	- UserName = DevUser
	- Schema = pilot
Update the README with instructions on how to switch between the two databases."

11.
Calling the `about` endpoint while passing true to the `show_details` parameter results in null configuration settings:
`{
  "apiVersion": "1.0.0",
  "applicationConfiguration": {
    "dataSources": null,
    "openApi": null,
    "active": true
  },
  "buildVersion": "1.0.0",
  "deployDate": "2026-08-16T17:18:07.162132+00:00",
  "name": "PilotApiPython"
}`.
Correct the application, so the configuration settings reflact the application configuration settings.

12.
Update the GetAll endpoints to add pagination, with two optional query parameters:
- `page` = the target page index.  1-based, meaning the first page is 1.
	- Default: 0
- `pageSize` = how many items return per page.
	- Default: 20
If `page` = 0, do not use paging - return all rows.
This pagination will control how data is retieved from the database.

12.
Update this application to work with Open Telemetry (OTEL).
OTEL collector is deployed to Docker container `otel-collector`, using the following ports:
- OTLP gRPC: 4317
- OTLP HTTP: 4318
OTEL deploy script: `C:\Working\Storage\Dev\GitHub\Docker\Otel\Docker-Otel-builder.bat`.
When the OTEL updates are complete, update the README to include details, such as what keywords would be used in Grafana to review logged events.


